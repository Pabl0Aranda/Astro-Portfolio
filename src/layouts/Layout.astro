---
import { ClientRouter } from "astro:transitions";
import "../styles/global.css";
import Header from "../components/Header.astro";
import CommandPalette from "../components/CommandPalette.astro";
import Search from "../components/Search.astro";
import BackToTop from "../components/BackToTop.astro";
import Chatbot from "../components/Chatbot.astro";
import "katex/dist/katex.min.css";

// 1. Añadimos 'image' como propiedad opcional
interface Props {
  title: string;
  description: string;
  image?: string;
  structuredData?: string;
}

// 2. Definimos una imagen por defecto (/og-image.jpg) si no le pasamos ninguna
const {
  title,
  description,
  image = "/og-image.jpg",
  structuredData,
} = Astro.props;

// 3. Construimos las URLs absolutas necesarias para el SEO
const canonicalURL = new URL(Astro.url.pathname, Astro.site || Astro.url);
const socialImageURL = new URL(image, canonicalURL);
const gtmId = import.meta.env.PUBLIC_GTM_ID;
---

<!doctype html>
<html lang="es" class="scroll-smooth md:cursor-none">
  <head>
    <!-- Google Tag Manager -->
    {
      gtmId && (
        <script
          is:inline
          set:html={`
          (function (w, d, s, l, i) {
            w[l] = w[l] || [];
            w[l].push({ "gtm.start": new Date().getTime(), event: "gtm.js" });
            var f = d.getElementsByTagName(s)[0],
              j = d.createElement(s),
              dl = l != "dataLayer" ? "&l=" + l : "";
            j.async = true;
            j.src = "https://www.googletagmanager.com/gtm.js?id=" + i + dl;
            f.parentNode.insertBefore(j, f);
          })(window, document, "script", "dataLayer", "${gtmId}");
        `}
        />
      )
    }
    <!-- End Google Tag Manager -->

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={socialImageURL} />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={socialImageURL} />

    {
      structuredData && (
        <script type="application/ld+json" set:html={structuredData} />
      )
    }

    <ClientRouter />

    <script is:inline>
      const applyTheme = () => {
        const theme =
          localStorage.getItem("theme") ??
          (window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light");

        if (theme === "dark") {
          document.documentElement.classList.add("dark");
        } else {
          document.documentElement.classList.remove("dark");
        }
      };
      applyTheme();

      document.addEventListener("astro:after-swap", applyTheme);

      document.addEventListener("astro:before-swap", (ev) => {
        if (document.documentElement.classList.contains("dark")) {
          ev.newDocument.documentElement.classList.add("dark");
        } else {
          ev.newDocument.documentElement.classList.remove("dark");
        }
      });
    </script>
  </head>
  <body
    class="selection:bg-albero group relative flex min-h-screen flex-col overflow-x-hidden bg-white text-neutral-900 antialiased transition-colors duration-300 selection:text-black dark:bg-black dark:text-white"
  >
    <!-- Google Tag Manager (noscript) -->
    {
      gtmId && (
        <noscript>
          <iframe
            src={"https://www.googletagmanager.com/ns.html?id=" + gtmId}
            height="0"
            width="0"
            style="display:none;visibility:hidden"
          />
        </noscript>
      )
    }
    <!-- End Google Tag Manager (noscript) -->
    <div
      id="custom-cursor"
      aria-hidden="true"
      class="bg-albero pointer-events-none fixed top-0 left-0 z-100 hidden h-2 w-2 rounded-full opacity-0 transition-all duration-300 ease-out group-hover:opacity-100 md:block"
      style="transform: translate(calc(var(--x) - 50%), calc(var(--y) - 50%)); will-change: transform;"
    >
    </div>

    <div class="pointer-events-none fixed inset-0 z-[-1] overflow-hidden">
      <div
        class="bg-size:4rem_4rem mask-image:radial-gradient(ellipse_80%_50%_at_50%_0%,#000_70%,transparent_110%) absolute inset-0 bg-[linear-gradient(to_right,#ffffff0a_1px,transparent_1px),linear-gradient(to_bottom,#ffffff0a_1px,transparent_1px)]"
      >
      </div>
      <div
        class="absolute top-0 left-0 -mt-[600px] -ml-[600px] h-[1200px] w-[1200px] opacity-0 transition-opacity duration-500 group-hover:opacity-100"
        style="background: radial-gradient(circle, rgba(229, 177, 58, 0.05) 0%, transparent 50%); transform: translate(var(--x, 50vw), var(--y, 50vh)); will-change: transform;"
      >
      </div>
    </div>

    <Search />

    <Header />

    <CommandPalette />

    <main
      transition:animate="slide"
      class="relative z-10 mx-auto w-full max-w-5xl grow px-6"
    >
      <slot />
    </main>

    <BackToTop />
    <Chatbot />
    <script>
      import VanillaTilt from "vanilla-tilt";

      document.addEventListener("astro:page-load", () => {
        const body = document.body;
        const customCursor = document.getElementById("custom-cursor");

        window.addEventListener("mousemove", (e) => {
          requestAnimationFrame(() => {
            body.style.setProperty("--x", `${e.clientX}px`);
            body.style.setProperty("--y", `${e.clientY}px`);
          });
        });

        if (customCursor) {
          document.addEventListener("mouseover", (e) => {
            const target = e.target as HTMLElement;
            if (target.closest("a") || target.closest("button")) {
              customCursor.classList.remove("h-2", "w-2", "bg-albero");
              customCursor.classList.add(
                "h-8",
                "w-8",
                "bg-transparent",
                "border",
                "border-albero",
              );
            }
          });

          document.addEventListener("mouseout", (e) => {
            const target = e.target as HTMLElement;
            if (target.closest("a") || target.closest("button")) {
              customCursor.classList.remove(
                "h-8",
                "w-8",
                "bg-transparent",
                "border",
                "border-albero",
              );
              customCursor.classList.add("h-2", "w-2", "bg-albero");
            }
          });
        }

        const observerOptions = {
          root: null,
          rootMargin: "0px",
          threshold: 0.15,
        };

        const observer = new IntersectionObserver((entries, observer) => {
          let delay = 0;
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              setTimeout(() => {
                entry.target.classList.add("is-visible");
              }, delay);
              delay += 100;
              observer.unobserve(entry.target);
            }
          });
        }, observerOptions);

        document
          .querySelectorAll(".reveal-text, .reveal-card")
          .forEach((el) => {
            observer.observe(el);
          });

        // Magnetic Buttons Logic
        document.querySelectorAll(".magnetic-btn").forEach((btn) => {
          let rafId: number | null = null;
          btn.addEventListener("mousemove", (e) => {
            if (rafId) cancelAnimationFrame(rafId);
            rafId = requestAnimationFrame(() => {
              const mouseEvent = e as MouseEvent;
              const rect = btn.getBoundingClientRect();
              const x = mouseEvent.clientX - rect.left - rect.width / 2;
              const y = mouseEvent.clientY - rect.top - rect.height / 2;
              (btn as HTMLElement).style.transform =
                `translate(${x * 0.2}px, ${y * 0.2}px)`;
            });
          });
          btn.addEventListener("mouseleave", () => {
            if (rafId) cancelAnimationFrame(rafId);
            (btn as HTMLElement).style.transform = `translate(0px, 0px)`;
          });
        });

        // VanillaTilt Initialization
        const tiltElements = Array.from(
          document.querySelectorAll(".tilt-card"),
        ) as HTMLElement[];
        if (tiltElements.length > 0) {
          VanillaTilt.init(tiltElements, {
            max: 5,
            speed: 400,
            glare: false,
            "max-glare": 0.1,
          });
        }
      });
    </script>
  </body>
</html>
